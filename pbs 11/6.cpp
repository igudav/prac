#include <iostream>
#include <map>
#include <vector>
#include <string>

using namespace std;

int main()
{
    map<int, string> labels;
    vector<string> ops;
    string cur;

    int n = 0;
    while (cin >> cur) {
        n++;
        ops.push_back(cur);
        if (cur[cur.size() - 1] == ':') {
        }
    }
}