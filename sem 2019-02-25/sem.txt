Грабли раскидали: перегрузка некоторых операций реализуется специфически

Цикл с префиксным инкрементом заведомо не хуже (мб лучше), чем соотв постфиксные

[] переопределяем, когда есть какой-то доступ к произвольному элементу коллекции/контейнера

. переопределять мы не можем, а -> можем. Но нафига? См. Proxy

Операция () - самая главная в с++

При переопределении , && || теряются все приколы с порядком вычисления и короткая логика