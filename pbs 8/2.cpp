#include <string>
#include <iostream>
#include <cctype>

using namespace std;

int main()
{
    char c;
    int pat = 0;
    int k = 0, m = 0;
    int index = 0;

    while (cin >> c) {
        if (isspace(c)) {
            continue;
        }
    }
}